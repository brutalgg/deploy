# Deploy Container

Simple minimal container that only 'has what I need' for deploying docker containers and stacks. Really just meant to save the step of having to install necessary tools each time a deploy job is run on generic build containers.
